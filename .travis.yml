addons:
  apt:
    sources:
      - ubuntu-toolchain-r-test
    packages:
      - cmake
      - libclang-3.4-dev
      - libnuma-dev
os:
  - linux
  - osx
language: cpp
cache: ccache
script:
  - ./ci/travis/build.sh
