version: 1.0.{build}
image: Visual Studio 2017
platform: x64
configuration: Debug
shallow_clone: true
clone_depth: 1
build:
  project: ab.sln
  parallel: true
  verbosity: minimal
test_script:
  - ps: ci\appveyor\build.ps
